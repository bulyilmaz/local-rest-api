### User Module with Local REST API (fetch, async, await) & Webpack

`cd /path/to/local-rest-api`

`npm install`

`npm run rest-api`

`npm run js-compile`

`npm run js-dev-compile`

`npm run watch`

`npm run dev-server`